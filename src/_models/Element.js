

class ELememt {

    constructor(element) {
        const { type, label, value } = element
        this.type = type
        this.label = label
        this.value = value
    }
}

export default ELememt